# Development guide

## Setup

1. Install `node` lastest version if needed
2. clone repo on download zip than
```bash
npm install
```

2. Develop the landing page

```bash

npm install

npm run dev
```

)
